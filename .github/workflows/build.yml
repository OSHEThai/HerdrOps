name: v0.6 Presentation and Export Gates

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  technical-gate:
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.x

      # Consolidated Static/Synthetic/Contract gate for Issue #34. This does
      # not execute or claim Actual Herdr Runtime or Release acceptance.
      - name: Run v0.6 version-local technical gate
        shell: pwsh
        run: ./tools/Test-V06TechnicalGate.ps1 -Configuration Release

      - name: Upload v0.6 version-local technical-gate evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-version-local-technical-gate
          path: |
            artifacts/release-gates/v0.6.0
            artifacts/test-results
          if-no-files-found: warn

  evaluation-gate:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.x

      - name: Run v0.6 Issue #31 Evaluation page gate
        shell: pwsh
        run: ./tools/Test-V06EvaluationPage.ps1 -Configuration Release

      - name: Upload v0.6 Issue #31 Evaluation gate
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-issue-31-evaluation-gate
          path: artifacts/release-gates/v0.6.0/issue-31
          if-no-files-found: warn

      - name: Upload v0.6 Issue #31 Evaluation design evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-issue-31-evaluation-design-evidence
          path: artifacts/design-evidence/v0.6.0/issue-31/evaluation
          if-no-files-found: warn

      - name: Upload v0.6 Issue #31 Evaluation test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-issue-31-evaluation-test-results
          path: artifacts/test-results
          if-no-files-found: ignore

  daily-summary-gate:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.x

      # Static/Synthetic/Contract only; Actual Herdr Runtime NOT OBSERVED.
      - name: 'Run v0.6 Daily Summary gate (Issue #32)'
        shell: pwsh
        run: ./tools/Test-V06DailySummaryPage.ps1 -Configuration Release

      - name: Upload Daily Summary gate report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-daily-summary-gate-report
          path: artifacts/release-gates/v0.6.0/issue-32
          if-no-files-found: warn

      - name: Upload Daily Summary deterministic artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-daily-summary-artifacts
          path: |
            artifacts/design-evidence/v0.6.0/issue-32
            artifacts/test-results

  local-export-gate:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.x

      # Static/Synthetic/Contract only; Actual Herdr Runtime NOT OBSERVED.
      - name: Run v0.6 local export acceptance gate
        shell: pwsh
        run: ./tools/Test-V06LocalExport.ps1 -Configuration Release

      - name: Upload v0.6 local export gate report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          retention-days: 7
          name: v0.6-local-export-gate-report
          path: artifacts/release-gates/v0.6.0/issue-33
          if-no-files-found: warn
