# Tests

Automated suites are separated into unit, contract, integration, and runtime-evidence projects. A green runtime test project does not by itself prove actual Herdr operation; test evidence must still identify whether it is static, synthetic, contract, or actual runtime evidence.
