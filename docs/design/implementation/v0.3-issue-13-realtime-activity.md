# v0.3 Issue #13 — Realtime Activity implementation checklist

Status: implementation ready; actual live-event acceptance pending

Authority: `docs/design/reference/03-realtime-activity.png` (`SHA-256 1EFBF8A309665511E6BF330E736D2B4CE96791338DB2101B7723B1A5C987BD0F`)

## Composition

- [x] Four summary cards retain the approved information hierarchy and status accents.
- [x] Time, role, severity, task, and evidence-level filters occupy one bounded filter row.
- [x] The event stream keeps time, event, task, actor, and severity visible in a dense virtualized list.
- [x] Selecting a row updates the event detail and evidence-source panels from the same event identity.
- [x] Bounded paging exposes seven rows at a time and never presents more than 32 retained events.
- [x] The shared HerdrOps logo, dark navy surfaces, blue borders, spacing, and typography tokens are preserved.

## Language separation

- [x] Thai mode displays Thai interface copy without paired English subtitles.
- [x] English mode displays English interface copy and contains no visible Thai glyphs.
- [x] Filter identity, selected event identity, task identifiers, filenames, and hashes remain stable when the language changes.
- [x] Product names, Agent identities, task identifiers, filenames, and paths remain technical data as defined by the v0.1 language decision.

## Deterministic behavior

- [x] The committed UI fixture is fixed to 14 August 2026 at 14:32:24 UTC+07:00.
- [x] Reapplying all five filter dimensions produces the same ordered event identities.
- [x] A filtered-out selection moves to the first visible event and keeps detail/evidence synchronized.
- [x] Loading more history preserves the first page, stable ordering, and unique event identities.
- [x] Production state fails closed with no events until an actual Core activity stream is connected.

## Visual evidence

- [x] Actual WPF rendering produces Thai and English 1672×941 captures.
- [x] Actual WPF rendering produces a Thai 1366×768 compact-layout capture.
- [x] A selected scope-violation capture proves synchronized detail and three evidence rows.
- [x] Automated checks reject the internal `ActivityFilterOption` representation in visible filter copy.
- [ ] An actual live event capture from the v0.3 collectors is recorded and independently accepted.

## Evidence locations

Actual WPF output backed by deterministic synthetic state:

```text
artifacts/design-evidence/v0.3.0/issue-13/contract-backed-wpf/
├── realtime-activity-th-1672x941.png
├── realtime-activity-th-scope-selected-1672x941.png
├── realtime-activity-th-1366x768.png
└── realtime-activity-en-1672x941.png
```

Gate output:

```text
artifacts/release-gates/v0.3.0/issue-13/<run-id>/gate-report.txt
```

## Evidence boundary

The state/filter tests are contract evidence. The PNGs are actual WPF rendering evidence backed by deterministic synthetic records. They prove layout, language selection, bounded filtering/paging, and selected-event synchronization. They do not prove an installed Herdr session, collector latency, actual process/file activity, actual secret redaction, or v0.3 release readiness. Issue #13 remains open until the required actual live-event capture is admitted.
