# v0.2 Installed Herdr Contract — Issue #6

Status: implementation and local installed-binary contract evidence complete; CI and independent review pending

## Admitted binary

- Release ID: `0.8.0-preview.2026-08-04-d78e3d3b5126-x86_64-pc-windows-msvc`
- Executable length: `20,654,592` bytes
- Executable SHA-256: `6F470DA358D6713B6BEBAB922FFB1F5FE1D3D288CC6F374C7DCA1B4A9837A542`
- Contract ID: `herdr-0.8.0-preview-readonly-monitoring-v1`
- Contract revision: `1`
- Contract-schema fingerprint SHA-256: `40C9859AB291715A80154C278545C7E020213D7F0FED5955321ED98E1C2D03DB`

The executable under `%LOCALAPPDATA%\Programs\Herdr\bin` is a junction into the exact release
directory above. Herdr does not expose Windows file-version metadata and this binary is not
Authenticode-signed, so the release-directory identity, exact bytes, PE header, RPC markers,
serialized-shape markers, and SHA-256 are validated together.

## Read-only v0.2 protocol surface

The admitted binary contains all required RPC markers:

- `session.snapshot`
- `workspace.list`
- `tab.list`
- `pane.list`
- `agent.list`
- `agent.get`
- `pane.process_info`
- `pane.read`
- `events.subscribe`

It also contains the expected serialized shape markers for `SessionSnapshot`, `WorkspaceInfo`,
`TabInfo`, `PaneInfo`, `AgentInfo`, `PaneProcessInfo`, `PaneReadParams`, `PaneReadResult`,
`EventsSubscribeParams`, `Subscription.PaneAgentStatusChanged`,
`PaneAgentStatusChangedEvent`, and `SubscriptionEventEnvelope`.

The contract-schema fingerprint is derived from the canonical contract ID, revision, admitted
release ID, RPC list, shape markers, and transport markers. It is not presented as a hash of a
schema document extracted from the executable; the executable itself is bound separately by its
exact SHA-256.

Transport markers require `HERDR_ENV`, `HERDR_SOCKET_PATH`, `HERDR_PANE_ID`, and the Windows
platform branch. The installed integration source embedded in the executable constructs a
Windows Named Pipe path from `HERDR_SOCKET_PATH`.

## Fail-closed policy

HerdrOps accepts only the exact release ID and binary SHA-256 listed above. A changed release,
changed hash, malformed PE header, unreadable file, missing RPC method, missing shape, or missing
transport marker returns a non-compatible result. The command exits with code `2` for
compatibility failures and code `64` for invalid command usage.

An updated Herdr binary requires a successor contract and fresh inspection. Compatibility is not
inferred from a semantically similar version string.

## Repeatable evidence

```powershell
./tools/Test-V02ProtocolContract.ps1
```

The command writes a JSON inspection, two fresh TRX files, and a gate report under
`artifacts/protocol-evidence/v0.2/issue-6/<run-id>/`.

## Evidence boundary

The installed-binary inspection is `Contract` evidence. It reads executable bytes only and does
not invoke the Herdr session-control CLI. It does not prove a live Named Pipe connection, actual
snapshot/event payloads, state semantics, disconnect/reconnect, reconciliation, or release
readiness. Those are version-local runtime gates for Issues #7–#10.
