# v0.4 Issue #18 independent review record

Verdict: PASS

ManifestPath: docs/reviews/v0.4-issue-18-reviewed-files.sha256
ManifestSha256: 5813D836EA2DE7BB10E43FD7AA387F935E7F1C23E8625DA903C743D89DCB45F0

## Reviewed scope

The independent read-only review covered the Issue #18 implementation through commit
`9917b78f1cc202fbb0c71656627fe6c4a780fcfe`. The reviewed scope included strict CLI
contracts, fail-closed validation, Core-owned acceptance identity, current-user-only
Named Pipe transport, idempotent retries, the CLI dependency boundary, lifecycle
evidence classification, the actual-runtime composite boundary, tests, documentation,
and implementation gates.

Merge commit `7b56c46fbef20f93be4f1c55d9b4f46d59c81e32` then incorporated the current `main`
baseline, including PR #80, without changing the Issue #18 implementation paths bound
by the reviewed-file manifest.

## Review history

The first review of commit `21e22df085941236a53f2c32cdcb90a96485306e`
returned `NEEDS_CHANGES` with one P1 finding: the actual-runtime harness still required
the obsolete lifecycle trace classification `Runtime` even though the producer and
composite command correctly used `BuiltProcessIntegration`.

Commit `9917b78f1cc202fbb0c71656627fe6c4a780fcfe` corrected the harness and added
`RuntimeHarnessRequiresTheBuiltProcessLifecycleClassification`, which requires the
producer, composite command, and runtime harness to remain aligned. The independent
re-review returned `APPROVE` with no P0, P1, P2, or P3 findings.

## Verified evidence

- PowerShell parser and format verification: PASS
- Full local suite: 383/383 PASS
- Issue #18 CLI self-report gate: 22/22 PASS
- Issue #19 lifecycle gate: 33/33 PASS
- Built CLI-to-Core trace classification: `BuiltProcessIntegration`
- Reviewed trace SHA-256: `2750DADC7B8956097671C51359A5DC49D167ED67AC5CEC728371ECCD1D79F111`

## Evidence decision

The reviewed implementation satisfies the Issue #18 contract, validation, dependency,
transport, acceptance-identity, and required-evidence criteria. The committed review
record therefore grants independent-review credit to Issue #18.

The PASS record is fail-closed against the committed reviewed-file manifest rather than
an informational Git-history reference, so the binding remains verifiable in shallow CI
checkouts. Any change to one of its 28 contract, source, example, test, harness, or gate
files invalidates the binding until a successor independent review records a new
manifest.

This verdict does not classify the built CLI-to-Core trace as actual Herdr runtime. It
does not prove an installed Herdr agent, role-distinct runtime acceptance, v0.4 release
readiness, package readiness, deployment, or publication. Those remain separate gates.
