# HerdrOps v0.7 Issue #37 Automated Agent Re-Review 2 (PASS)

Reviewer: Kilo (automated coding agent)
ReviewClassification: AutomatedAgentReview
ReviewPurpose: Fail-closed canonical manifest verifier remediation
ReviewedUtc: 2026-08-20T17:13:59Z
Verdict: PASS (automated Static/Synthetic scope only)
Findings: 0 P0, 0 P1, 0 P2, 0 P3 within this scope
HumanIndependentReview: PENDING / NOT OBSERVED
RequiredHumanApproval: PENDING / NOT OBSERVED

This is an automated-agent review record. It does not claim human independence,
human review, or human approval. Automated reviewer identity and Git author
identity are not evidence of a distinct human reviewer.

ReviewedCommit: c0a964e0ac45d451c672e5cc0ec77f9eb06466da
ReviewedBranch: codex/v07-issue-37-independent-review
BaseCommit: 2906eecfa1e12a5f039b05b860f5ebf3b87bcc61
ReviewWorktree: Z:/HerdrOps/.worktrees/v07-issue-37-independent-review
WorktreeStatusAtReview: CLEAN
Issue: #37

## Remediation scope

The verifier at `tools/Test-V07Issue37ManifestIntegrity.ps1` now fails closed
unless the manifest blob at the exact reviewed `HEAD` equals the pinned
remediation blob anchor:

```text
ManifestPath: docs/reviews/v0.7-issue-37-reviewed-files.sha256
PinnedManifestBlobSha1: 8bfe715043cd2a3935ac3e1b3a8abeb87c856431
PinnedManifestSha256: 633FE04B260CFABC02D19E6AB69D32E2D5E2621AFCAEDBD67691CB8ADEB3B7F7
ReviewedHEADManifestBlobSha1: 8bfe715043cd2a3935ac3e1b3a8abeb87c856431
HEADManifestAnchor: PASS
```

The strict parser uses an ordinal allowlist of the 35 canonical paths and
rejects all of the following before per-file hash verification:

- malformed or blank records;
- duplicate paths;
- unexpected paths;
- missing canonical paths; and
- any entry count other than exactly 35.

The verifier reads the manifest bytes from the reviewed `HEAD` blob only after
the anchor check passes. The per-file hashes remain pinned to the candidate
commit, with `StateStoreRecoveryTests.cs` pinned to the remediation commit as
recorded by the original manifest.

## Focused safe evidence

Commands executed at `ReviewedCommit`:

```text
pwsh -NoProfile -File tools/Test-V07Issue37ManifestIntegrity.ps1 -SelfTest
Result: PASS — valid fixture plus five deterministic negative cases and anchor mismatch.

pwsh -NoProfile -File tools/Test-V07Issue37ManifestIntegrity.ps1
Result: PASS — 41 passed, 0 failed; 35 entries, 0 per-file failures; exit 0.

git diff --check
Result: PASS
```

The deterministic self-tests cover malformed, duplicate, unexpected, missing,
wrong-count, and `HEAD`-anchor-mismatch rejection using in-memory fixtures.
No repository file is written by `-SelfTest`.

No `dotnet test`, runtime acceptance gate, installed Herdr process, live pane,
packaged artifact, clean-machine install, or Release gate was run or observed.

## Evidence boundary

| Class | Status | Detail |
|---|---|---|
| Static | OBSERVED | Verifier source, pinned constants, exact path allowlist, and fail-closed control flow reviewed at `ReviewedCommit`. |
| Synthetic | OBSERVED | Normal verifier 41/41 and deterministic negative self-tests passed. |
| Contract | NOT ASSESSED | No protocol or named-pipe contract was changed by this remediation. |
| Runtime | NOT OBSERVED | No actual Herdr process or runtime acceptance was used. |
| Release | NOT CLAIMED | No package, clean-machine install, release gate, or human approval was produced. |

No Runtime or Release credit is claimed. This record does not close Issue #37.

## Verdict

The verifier remediation is accepted for automated Static/Synthetic evidence at
`ReviewedCommit` c0a964e. The prior human-independent-review and approval
requirements remain pending and are not satisfied by this automated record.

AutomatedReReview: COMPLETE
HumanIndependentReview: PENDING / NOT OBSERVED
RequiredHumanApproval: PENDING / NOT OBSERVED
ActualHerdrRuntime: NOT OBSERVED
ReleaseStatus: NOT CLAIMED
NoRuntimeCredit: TRUE
